name: flutter1
description: Basic dictionary lookup application, using the DictionaryX library

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  get: ^4.6.5
  dictionaryx: ^0.3.5
  provider: ^6.0.0
  settings_ui: ^2.0.2
  shared_preferences: ^2.2.1
  sqflite: ^2.3.0
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true